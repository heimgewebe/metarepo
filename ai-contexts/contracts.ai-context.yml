---
ai_context_version: 1.1

# COMPATIBILITY SHIM / ALIAS
# Canonical file: ai-contexts/contracts-mirror.ai-context.yml
# Do not update this file except to keep the alias pointing to canonical truth.

project:
  name: contracts
  summary: "Alias for contracts-mirror. Canonical: ai-contexts/contracts-mirror.ai-context.yml"
  role: compatibility_alias
  primary_language: mixed
  visibility: internal

boundaries:
  not_responsible_for:
    - "Being canonical. Use contracts-mirror.ai-context.yml."

ai_guidance:
  do:
    - "Use contracts-mirror.ai-context.yml instead."
  dont:
    - "Update this file."
