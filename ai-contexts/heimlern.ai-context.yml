ai_context_version: 1.1
project:
  name: heimlern
  summary: Archived historical reference for the former pattern-learning and policy-adaptation
    repository.
  role: archived_reference
  primary_language: mixed
  visibility: internal
heimgewebe:
  axis:
  - Historical Learning
  - Policy Evidence
  fleet:
    enabled: false
  wgx:
    profile_expected: false
    guard_smoke_expected: false
interfaces:
  consumes:
  - historical contract evidence only
  produces:
  - none (archived repository)
contracts:
  schemas:
  - path: contracts/*.schema.json
    note: Policy loop must stay schema-bound; no silent drift.
dependencies:
  internal: []
  external: []
boundaries:
  not_responsible_for:
  - Runtime execution, routing, queue consumption, production writes, or Fleet operations.
  dangerous_assumptions:
  - Treat this archived reference as an active policy engine or development target.
conventions:
  branching: archived; no active development branches
  commit_prefix: heimlern
  ci_platform: github_actions
documentation:
  adr: docs/adrs/
ai_guidance:
  do:
  - Use the exact archived commit and archive-readiness evidence for historical analysis.
  dont:
  - Propose runtime integrations, Fleet rollout, dispatch, or new feature development
    here.
