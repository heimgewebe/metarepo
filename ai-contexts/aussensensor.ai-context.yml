---
ai_context_version: 1.1
project:
  name: aussensensor
  summary: Archived historical reference for the deleted former controlled external-ingest
    component; not a current Fleet or runtime component.
  role: archived_reference
  primary_language: mixed
  visibility: internal
heimgewebe:
  axis:
  - Historical External Ingest Evidence
  fleet:
    enabled: false
  wgx:
    profile_expected: false
    guard_smoke_expected: false
interfaces:
  consumes:
  - historical repository evidence only
  produces:
  - none (deleted repository)
contracts:
  schemas:
  - path: contracts/*.schema.json
    note: Historical schema references do not establish a current producer or consumer.
dependencies:
  internal: []
  external: []
boundaries:
  not_responsible_for:
  - Current runtime execution, Fleet rollout, deployment, or producer/consumer truth.
  dangerous_assumptions:
  - Treat aussensensor as a current repository, service, Fleet member, producer, or
    consumer.
conventions:
  branching: archived reference; repository physically deleted
  commit_prefix: aussensensor
  ci_platform: none
ai_guidance:
  do:
  - Use this file only as historical context and verify current successors elsewhere.
  dont:
  - Create current work, routing, deployment, or contract claims for the deleted repository.
