---
ai_context_version: 1.1

project:
  name: hausKI-audio
  summary: Historical donor repository retained for ideas, behavior, and validation evidence; not the current Audio product.
  role: historical_donor
  primary_language: mixed
  visibility: internal

heimgewebe:
  axis: [Historical Audio Evidence]
  fleet:
    enabled: false
  wgx:
    profile_expected: false
    guard_smoke_expected: false

interfaces:
  consumes:
    - historical repository evidence only
  produces:
    - none (historical donor)

contracts:
  schemas:
    - path: contracts/*.schema.json
      note: Historical validation material does not establish a current producer.

dependencies:
  internal: []
  external: []

boundaries:
  not_responsible_for:
    - Current audio configuration, playback, recording, instruments, or music experiments.
    - Fleet rollout, runtime execution, event delivery, or current producer claims.
  dangerous_assumptions:
    - Treat this donor repository as the canonical Audio product.
    - Infer a live audio.events producer from a validation workflow.

conventions:
  branching: historical donor; no new product work
  commit_prefix: hausKI-audio
  ci_platform: github_actions

documentation:
  repository: https://github.com/heimgewebe/hausKI-audio

source_binding:
  repository: heimgewebe/hausKI-audio
  commit: 4f2b4bdbfaa6419725aba02acd71347ac1e9da02
  observed_at: '2026-07-29T04:45:00Z'

ai_guidance:
  do:
    - Use exact revisions when extracting historical requirements or tests.
    - Reimplement useful behavior against the canonical audio contracts.
  dont:
    - Route new implementation, Fleet, Integrity, or runtime work to this repository.
    - Claim that its old event schema has current producers or consumers.
