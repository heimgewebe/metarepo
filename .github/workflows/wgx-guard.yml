name: wgx-guard

permissions:
  contents: read

on:
  pull_request:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  guard:
    # metarepo wird selbst wie ein Fleet-Repo behandelt und nutzt
    # die kanonische Guard-Definition aus heimgewebe/wgx.
    # HINWEIS: @main wird akzeptiert, da wgx noch keine stabilen Tags hat.
    # Sobald Tags verfügbar sind, sollte dies auf einen stabilen Tag gepinnt werden.
    uses: heimgewebe/wgx/.github/workflows/reusable-wgx-guard.yml@main
