---
# managed-by: metarepo-sync
name: wgx-guard

permissions:
  contents: read

"on":
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  guard:
    uses: ./.github/workflows/reusable-repo-verify.yml
    with:
      mode: guard
