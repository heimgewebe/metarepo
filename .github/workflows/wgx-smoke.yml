---
# managed-by: metarepo-sync
name: wgx-smoke

permissions:
  contents: read

"on":
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  smoke:
    uses: ./.github/workflows/reusable-repo-verify.yml
    with:
      mode: smoke
