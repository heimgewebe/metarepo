diff --git a/scripts/wgx b/scripts/wgx
new file mode 100755
index 0000000..baf9a9c
--- /dev/null
+++ b/scripts/wgx
@@ -0,0 +1,12 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+# Kompatibilitäts-Shim zum neuen Bash-Dispatcher unter wgx/wgx.
+# Viele Call-Sites (Justfile, ältere Repos, CI) sprechen noch scripts/wgx an.
+
+ROOT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
+DISPATCHER="$ROOT_DIR/wgx/wgx"
+
+if [[ ! -x "$DISPATCHER" ]]; then
+  echo "ERR: expected wgx dispatcher at $DISPATCHER" >&2
+  exit 127
+fi
+exec "$DISPATCHER" "$@"
diff --git a/templates/.github/workflows/wgx-guard.yml b/templates/.github/workflows/wgx-guard.yml
new file mode 100644
index 0000000..8f5c6a5
--- /dev/null
+++ b/templates/.github/workflows/wgx-guard.yml
@@ -0,0 +1,20 @@
+name: wgx-guard
+
+on:
+  pull_request:
+  push:
+    branches: [ main ]
+  workflow_dispatch:
+
+permissions:
+  contents: read
+
+jobs:
+  guard:
+    # Flottenweiter Guard ruft den im Metarepo definierten
+    # reusable Workflow auf, der wiederum ./wgx/wgx guard nutzt.
+    uses: heimgewebe/metarepo/.github/workflows/reusable-wgx-guard.yml@main
diff --git a/templates/.github/workflows/wgx-smoke.yml b/templates/.github/workflows/wgx-smoke.yml
new file mode 100644
index 0000000..a4a2b33
--- /dev/null
+++ b/templates/.github/workflows/wgx-smoke.yml
@@ -0,0 +1,20 @@
+name: wgx-smoke
+
+on:
+  pull_request:
+  push:
+    branches: [ main ]
+  workflow_dispatch:
+
+permissions:
+  contents: read
+
+jobs:
+  smoke:
+    # Flottenweiter Smoke ruft den im Metarepo definierten
+    # reusable Workflow auf, der wiederum ./wgx/wgx smoke nutzt.
+    uses: heimgewebe/metarepo/.github/workflows/reusable-wgx-smoke.yml@main
